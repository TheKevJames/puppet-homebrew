# Local Agent Instructions

## Repository Conventions
- Store repository helper scripts in `./bin`.
- Do not add new project scripts under `./script` or `./scripts`.

## Validation
- Run `pre-commit run -a` after making changes.
