dependencies:
  post:
    - bundle exec puppet module build
    - bundle exec puppet module install pkg/thekevjames-homebrew-*.tar.gz

test:
  override:
    - bundle exec rake validate
    # Only works on OSX
    # - bundle exec puppet apply --noop tests
